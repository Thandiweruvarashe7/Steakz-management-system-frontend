# Production — deployed Render backend
VITE_API_URL=https://steakz-management-system-backend.onrender.com/api

# Local development — uncomment and use a .env.local file to override
# VITE_API_URL=/api
# (The Vite dev-server proxy in vite.config.ts will forward /api → http://localhost:5000)

VITE_APP_NAME=STEAKZ UK
